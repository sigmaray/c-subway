/* Convenience include for platform/render — all public APIs live in metro.h.
 * Compile with -Iinclude (or include metro.h directly). */
#ifndef GAME_API_H
#define GAME_API_H
#include "metro.h"
#endif
